FROM node:18

ARG BACKEND_URL
ARG FRONTEND_URL

WORKDIR /usr/app

ENV NEXT_PUBLIC_BACKEND_URL=${BACKEND_URL}
ENV NEXT_PUBLIC_FRONTEND_URL=${FRONTEND_URL}}

COPY package*.json ./

RUN npm ci

COPY . .

RUN npm run build

EXPOSE 3000

CMD ["npm", "run", "start"]